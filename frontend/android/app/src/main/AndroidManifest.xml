<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frontend">


    <!-- Network access for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Vibration permission for breach alerts -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Needed when plugins start a foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!--
      NOTE:
      - If you ever need location in the background (when the app isn't visible),
        you'll also need this AND extra handling in code & Play Console:
        <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
      - Not required for our current foreground SOS use case.
    -->

    <application
        android:label="Safety Mitra"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Visible immediately while Flutter initializes -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Required by Flutter embedding (Android 12+ okay) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC_YNpPL2LexQxSuuTl_MGDcA07JhwyBUU"/>

        <!-- FCM service placeholder (FirebaseMessagingService subclasses get registered automatically
             by the AndroidManifest merging when you integrate firebase_messaging; this is a safe placeholder) -->
        <service android:name="io.flutter.plugins.firebasemessaging.FlutterFirebaseMessagingService"
                 android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

    <!-- Package visibility (used by Flutter text processing plugin) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
